import { DynamicDashDirective } from './dynamic-dash.directive';

describe('DynamicDashDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicDashDirective();
    expect(directive).toBeTruthy();
  });
});
